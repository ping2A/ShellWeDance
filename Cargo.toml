# Virtual workspace: PowerShell WASM analyzer only (shell-we-dance-ps + wasm UI crate)
[workspace]
members = ["shell-we-dance-ps", "wasm"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
